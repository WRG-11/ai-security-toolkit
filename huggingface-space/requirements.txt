# Only for the local Gradio variant (`app.py`). A Static Space ignores this
# file -- index.html installs the package in the browser through micropip.
#
# The package is the dependency rather than vendored code: `tools` is the
# installable surface and the trained model ships with it as package data.
gradio>=4.0.0
wrg-ai-security-toolkit>=0.3.0
